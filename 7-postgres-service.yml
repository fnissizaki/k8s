apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: db-service
    app: demo-voting-app
spec:
  selector:
    name: postgres-deployment
    app: demo-voting-app
  ports:
  - port: 5432
    targetPort: 5432
